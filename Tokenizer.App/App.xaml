<Application
    x:Class="Tokenizer.App.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    RequestedTheme="Dark">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
            </ResourceDictionary.MergedDictionaries>

            <Color x:Key="AccentColor">#FF35B7FF</Color>
            <Color x:Key="AccentColorMuted">#5535B7FF</Color>
            <Color x:Key="SurfaceColor">#FF0B0D10</Color>
            <Color x:Key="SurfaceRaisedColor">#FF15191E</Color>
            <Color x:Key="SurfaceContrastColor">#FFF5F7FA</Color>
            <Color x:Key="SurfaceSoftColor">#FF111418</Color>
            <Color x:Key="WarmHighlightColor">#FFFFB454</Color>
            <Color x:Key="OutlineColor">#FF222831</Color>

            <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}" />
            <SolidColorBrush x:Key="AccentMutedBrush" Color="{StaticResource AccentColorMuted}" />
            <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource SurfaceColor}" />
            <SolidColorBrush x:Key="SurfaceRaisedBrush" Color="{StaticResource SurfaceRaisedColor}" />
            <SolidColorBrush x:Key="SurfaceContrastBrush" Color="{StaticResource SurfaceContrastColor}" />
            <SolidColorBrush x:Key="SurfaceSoftBrush" Color="{StaticResource SurfaceSoftColor}" />
            <SolidColorBrush x:Key="WarmHighlightBrush" Color="{StaticResource WarmHighlightColor}" />
            <SolidColorBrush x:Key="OutlineBrush" Color="{StaticResource OutlineColor}" />

            <Style x:Key="ShellCardStyle" TargetType="Border">
                <Setter Property="Background" Value="{StaticResource SurfaceRaisedBrush}" />
                <Setter Property="CornerRadius" Value="20" />
                <Setter Property="Padding" Value="20" />
                <Setter Property="BorderBrush" Value="{StaticResource OutlineBrush}" />
                <Setter Property="BorderThickness" Value="1" />
            </Style>

            <Style TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource SurfaceContrastBrush}" />
            </Style>

            <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Bahnschrift SemiBold" />
                <Setter Property="FontSize" Value="22" />
                <Setter Property="Foreground" Value="{StaticResource SurfaceContrastBrush}" />
            </Style>

            <Style x:Key="CardTitleStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Bahnschrift SemiBold" />
                <Setter Property="FontSize" Value="18" />
                <Setter Property="Foreground" Value="{StaticResource SurfaceContrastBrush}" />
            </Style>

            <Style x:Key="SectionSubtitleStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Opacity" Value="0.72" />
                <Setter Property="TextWrapping" Value="WrapWholeWords" />
                <Setter Property="Foreground" Value="{StaticResource SurfaceContrastBrush}" />
            </Style>

            <Style x:Key="SettingLabelStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Bahnschrift SemiBold" />
                <Setter Property="FontSize" Value="15" />
                <Setter Property="Foreground" Value="{StaticResource SurfaceContrastBrush}" />
            </Style>

            <Style x:Key="MetricValueStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Bahnschrift SemiBold" />
                <Setter Property="FontSize" Value="30" />
                <Setter Property="Foreground" Value="{StaticResource SurfaceContrastBrush}" />
            </Style>

            <Style x:Key="MetricCaptionStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="13" />
                <Setter Property="Opacity" Value="0.72" />
                <Setter Property="Foreground" Value="{StaticResource SurfaceContrastBrush}" />
            </Style>

            <Style x:Key="ShellNavButtonStyle" TargetType="Button">
                <Setter Property="Width" Value="84" />
                <Setter Property="MinWidth" Value="84" />
                <Setter Property="Height" Value="84" />
                <Setter Property="MinHeight" Value="84" />
                <Setter Property="Padding" Value="14,12" />
                <Setter Property="CornerRadius" Value="18" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="BorderBrush" Value="{StaticResource OutlineBrush}" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="Foreground" Value="{StaticResource SurfaceContrastBrush}" />
                <Setter Property="HorizontalAlignment" Value="Center" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>

