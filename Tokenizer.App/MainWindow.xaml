<Window
    x:Class="Tokenizer.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid
        x:Name="WindowRoot"
        Background="{StaticResource SurfaceBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="48" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid
            x:Name="TitleBarDragRegion"
            Grid.Row="0"
            Background="{StaticResource SurfaceBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="20,0,0,0">
                <Ellipse Width="10" Height="10" Fill="{StaticResource WarmHighlightBrush}" />
                <TextBlock
                    Margin="12,0,0,0"
                    FontFamily="Bahnschrift SemiBold"
                    FontSize="18"
                    Foreground="{StaticResource SurfaceContrastBrush}"
                    Text="{Binding AppTitle}" />
                <TextBlock
                    Margin="10,2,0,0"
                    FontSize="12"
                    Opacity="0.7"
                    Text="{Binding Subtitle}" />
            </StackPanel>
        </Grid>

        <Grid
            Grid.Row="1"
            ColumnSpacing="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="136" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border
                Grid.Column="0"
                Margin="16,12,0,20"
                Padding="14,20"
                Background="{StaticResource SurfaceSoftBrush}"
                BorderBrush="{StaticResource OutlineBrush}"
                BorderThickness="1"
                CornerRadius="24">
                <StackPanel Spacing="8">
                    <Button
                        x:Name="TodayNavButton"
                        Tag="today"
                        Click="NavButton_Click"
                        Style="{StaticResource ShellNavButtonStyle}">
                        <StackPanel HorizontalAlignment="Center" Spacing="4">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE80F;" />
                            <TextBlock FontSize="12" HorizontalTextAlignment="Center" Text="Today" />
                        </StackPanel>
                    </Button>

                    <Button
                        x:Name="HistoryNavButton"
                        Tag="history"
                        Click="NavButton_Click"
                        Style="{StaticResource ShellNavButtonStyle}">
                        <StackPanel HorizontalAlignment="Center" Spacing="4">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE81C;" />
                            <TextBlock FontSize="12" HorizontalTextAlignment="Center" Text="History" />
                        </StackPanel>
                    </Button>

                    <Button
                        x:Name="SettingsNavButton"
                        Tag="settings"
                        Click="NavButton_Click"
                        Style="{StaticResource ShellNavButtonStyle}">
                        <StackPanel HorizontalAlignment="Center" Spacing="4">
                            <FontIcon FontFamily="Segoe MDL2 Assets" Glyph="&#xE713;" />
                            <TextBlock FontSize="12" HorizontalTextAlignment="Center" Text="Settings" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <Grid
                x:Name="ShellContentArea"
                Grid.Column="1"
                Margin="0,12,20,20"
                Background="Transparent">
                <Grid
                    x:Name="PageHostContainer"
                    Background="Transparent">
                    <ScrollViewer
                        x:Name="PageViewport"
                        Background="Transparent"
                        HorizontalScrollMode="Disabled"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Stretch"
                        VerticalScrollMode="Disabled"
                        HorizontalScrollBarVisibility="Disabled"
                        VerticalScrollBarVisibility="Auto">
                        <ContentControl
                            x:Name="PageHost"
                            HorizontalAlignment="Stretch"
                            HorizontalContentAlignment="Stretch"
                            VerticalAlignment="Top"
                            VerticalContentAlignment="Top" />
                    </ScrollViewer>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Window>

